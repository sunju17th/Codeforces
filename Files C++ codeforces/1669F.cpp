#include<bits/stdc++.h>

using namespace std;

int main(){
    int t;
    cin >> t;
    while(t--)
    {
        int n;
        cin >> n;
        long long a[200005];
        for(int i=0;i<n;i++)
            cin >> a[i];

        long long l=0, r=n-1, sl=0, sr=0;

        while(l<r)
        {
            if()
        }
    }
}